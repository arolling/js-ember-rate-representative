import { ratingStars } from 'rate-your-representative/helpers/rating-stars';
import { module, test } from 'qunit';

module('Unit | Helper | rating stars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingStars([42]);
  assert.ok(result);
});
