import { alreadyAdded } from 'rate-your-representative/helpers/already-added';
import { module, test } from 'qunit';

module('Unit | Helper | already added');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = alreadyAdded([42]);
  assert.ok(result);
});
